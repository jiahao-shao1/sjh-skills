# Skeleton Manifest

Complete file listing generated by `init-skeleton.sh`.

## Directories

| Path | Purpose |
|------|---------|
| `.claude/rules/` | Hard rules (distilled from validated experience) |
| `docs/knowledge/` | Hot experience (debug findings, workarounds) |
| `.claude/hooks/` | PostToolUse automation hooks |
| `.claude/agents/` | Agent definitions (domain experts, quality checks) |
| `.claude/worktrees/` | Worktree tracking |
| `.agents/skills/` | Project-level skill definitions |
| `scripts/` | Project scripts (subdirs added by profile overlay) |

## Files

| Path | Type | Content |
|------|------|---------|
| `.claude/hooks/auto-format-python.sh` | Hook (Edit\|Write) | Auto `ruff format` + `check --fix` after editing `.py` files |
| `.claude/hooks/guard-critical-edit.sh` | Hook (Edit\|Write) | Warn when editing critical directories (customizable list) |
| `.claude/hooks/post-knowledge-remind.sh` | Hook (Bash) | Remind to capture debugging experience on command errors; frequency-limited, outputs systemMessage JSON |
| `.claude/rules/testing.md` | Rule | Universal test conventions (AAA pattern, markers, commands) |
| `.claude/rules/knowledge-writing.md` | Rule | Knowledge entry format, capture guidelines, progressive disclosure pattern |
| `.claude/agents/code-verifier.md` | Agent | ruff lint/format + pytest; `permissionMode: bypassPermissions`, `maxTurns: 15` |
| `.claude/agents/planner.md` | Agent | Codebase researcher; `permissionMode: plan` (read-only) |
| `.claude/settings.json` | Config | PostToolUse hooks: Edit\|Write (guard + format) + Bash (knowledge remind) |
| `CLAUDE.md` | Guide | Project guide skeleton (generic sections + placeholders) |
| `AGENTS.md` | Symlink | → `CLAUDE.md` — Codex reads this for project instructions |

## CLAUDE.md Skeleton Section Status

| Section | Status | Notes |
|---------|--------|-------|
| Project overview | Placeholder | Phase 2 interactive fill |
| Models / Key dependencies | Placeholder | Phase 2 interactive fill |
| Directory structure | Placeholder | Phase 2 interactive fill |
| Dev workflow | Partial | Generic flow written; project-specific parts are placeholders |
| Dev guide | Placeholder | Phase 2 interactive fill |
| Experience capture | Complete | Generic template, written directly |
| Context management | Complete | Generic template, written directly |
| Behavior boundaries | Partial | Generic items written; project-specific items are placeholders |
| Knowledge discovery | Complete | Progressive disclosure via inline refs in rules (no centralized table) |

## Idempotent Behavior

- Existing file → skip, never overwrite
- Existing directory → ignore
- Summary output distinguishes "created" vs "skipped"
